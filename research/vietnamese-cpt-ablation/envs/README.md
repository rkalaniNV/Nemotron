# Local Environment Profiles

The `*.toml` files in this directory are intentionally ignored because they
contain local Lepton settings and credentials such as W&B and Hugging Face
tokens.

Create local copies from your cluster/runtime setup when launching the ablation.
Do not commit credential-bearing environment files.
