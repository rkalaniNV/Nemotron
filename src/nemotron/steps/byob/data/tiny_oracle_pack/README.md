# Tiny library oracle pack (non-banking)

Bundled reference pack for BFCL validation and benchmark-family-agnostic tests.
Offline only — no network. Process-worker isolation is required for gold claims.

Pack id: `tiny_library`
Tools: `get_book_status`, `checkout_book` (confirmation opt-in)
Absent book id (never inserted): `BK-ABSENT-1`
