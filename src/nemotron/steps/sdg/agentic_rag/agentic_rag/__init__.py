"""Agentic-RAG deep-research trajectory SDG plugin (NDD column generator).

Corpus-side tooling (chunker, retriever, bundle builder) lives in ``data_prep/``.
This package is the runtime plugin: config, context management, prompts, tool
backends, and the phased deep-research simulation loop.
"""
