# Install into a Python 3.12 venv — nemo-retriever[local] requires >=3.12,<3.13:
#   uv venv --python 3.12 .venv
#   uv pip install --python .venv/bin/python -r requirements.txt
#
# torch must come from the PyTorch CUDA index below, NOT PyPI's default (which resolves
# to an incompatible build). cu128 = CUDA 12.8; swap to cu126/cu129/cu130 to match the
# host driver. Needs a platform with a matching torch 2.11.0 wheel (linux x86_64/aarch64,
# for the venv's Python). vllm 0.20.0 pins torch==2.11.0 with its cu12 libs, so keep them aligned.
--extra-index-url https://download.pytorch.org/whl/cu128

nemo-retriever[local]==26.5.0
vllm==0.20.0
torch==2.11.0
fastapi==0.138.0
uvicorn==0.51.0
lancedb==0.34.0
pandas==2.3.3
httpx==0.28.1
prometheus-fastapi-instrumentator==7.1.0
